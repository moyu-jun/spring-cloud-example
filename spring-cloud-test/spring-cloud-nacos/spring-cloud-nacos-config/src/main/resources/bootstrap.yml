server:
  port: 8081

spring:
  application:
    name: spring-cloud-nacos-config
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        prefix: spring-cloud-nacos-config
        file-extension: yaml